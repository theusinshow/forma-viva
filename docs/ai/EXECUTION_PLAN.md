# Execution Plan

> Generated by Coded by M — Site Architecture System. Source of truth: `/docs/ai/DISCOVERY.md` and `/docs/ai/DECISIONS_LOG.md`.

## Required Reading Order
1. `/CLAUDE.md`
2. `/docs/ai/MASTER_CONTEXT.md`
3. `/docs/ai/PROJECT.md`
4. `/docs/ai/SITE_ARCHITECTURE.md`
5. `/docs/ai/PAGE_BLUEPRINTS.md`
6. `/docs/ai/DESIGN_SYSTEM.md`
7. `/docs/ai/MOTION_DIRECTION.md`
8. `/docs/ai/QA_CHECKLIST.md`

## How to use skills in each phase
Session mode: **A (Completo)** — use recommended skills in any phase when they improve the result. Before invoking any skill: confirm it's installed, propose it, state expected output, ask approval. Never run two overlapping visual skills on the same decision (`impeccable` vs `frontend-design`/`ui-ux-pro-max`).

## Phase 01 — Project foundation
### Goal
Inicializar Next.js (App Router) + TypeScript + Tailwind. Configurar tokens (`:root` + `tailwind.config`), fontes (`next/font` Raleway + Inter), Lenis (smooth scroll), estrutura de pastas, layout global (navbar/footer), e estilos base dark-first.
### Recommended skill
`react-best-practices` (fallback: `references/stack-selection.md`).
### Execution Prompt
Read all required docs. Diagnose before editing. Implement only this phase. Report changed files, decisions and pending issues.

## Phase 02 — Essential components
### Goal
Construir o set de `DESIGN_SYSTEM.md`: Navbar, Footer, CTA Corner, Project Card editorial, Image Reveal, Case Header, Ficha Técnica, Text Block, Gallery Grid, Horizontal Gallery, Process Step, Contact Form, Cursor Label, Image Caption, Project Filter, Next Project Block, Page Transition.
### Recommended skill
`composition-patterns` para APIs de componentes; `design-system` se os tokens precisarem de estrutura.

## Phase 03 — Page structure
### Goal
Montar páginas e seções por `SITE_ARCHITECTURE.md` + `PAGE_BLUEPRINTS.md`. Criar dados MDX/Markdown dos 5 projetos. Roteamento `/projetos/[slug]`.
### Recommended skill
`frontend-design` para estrutura rápida (polimento premium vem na Fase 04).

## Phase 04 — Visual direction pass
### Goal
Aplicar o craft premium: "Galeria de Cantos", respiro, tipografia Raleway tracked, composições assimétricas, disciplina dark-first.
### Recommended skill
`impeccable` como executor visual primário. Não rodar `frontend-design`/`ui-ux-pro-max` na mesma decisão.

## Phase 05 — Motion and interactions
### Goal
Implementar motion por `MOTION_DIRECTION.md`: reveals on-scroll (GSAP/ScrollTrigger), page transition com imagem expandindo, hover zoom, cursor label, smooth scroll. Respeitar `prefers-reduced-motion`.
### Recommended skill
`impeccable` motion. Sem WebGL/3D.

## Phase 06 — Responsive pass
### Goal
Validar e refinar desktop/notebook/tablet/mobile. Colapsar composições de canto no mobile, tap targets ≥44px.
### Recommended skill
`webapp-testing` para checagens responsivas, fluxos e formulário.

## Phase 07 — Quality pass
### Goal
Acessibilidade (AA), performance (CWV — crítico, image-heavy), SEO (local SC), revisão final. Gate contra `QA_CHECKLIST.md`.
### Recommended skill
`web-design-guidelines` (UI/UX/a11y) + `react-best-practices` (performance).

## Optional — Deploy
Após aprovação do QA: `deploy-to-vercel`.

## Completion Criteria
`/docs/ai/QA_CHECKLIST.md` como gate de prontidão.

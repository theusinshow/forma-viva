# QA Checklist

> Generated by Coded by M — Site Architecture System. Source of truth: `/docs/ai/DISCOVERY.md` and `/docs/ai/DECISIONS_LOG.md`.
> GATE, not a suggestion. Phase 07 não conclui até cada item aplicável estar marcado ou dispensado em `DECISIONS_LOG.md` com motivo.

> **Status 2026-06-13:** MVP implementado e validado por build + screenshots (desktop/mobile).
> Waivers de demo: imagens reais (placeholders picsum — CNT-001..003), backend do formulário
> (front-only), logo SVG (wordmark tipográfico). Lighthouse formal fica para o deploy.

## Structural verification
- [x] Rotas existem e batem com `SITE_ARCHITECTURE.md` — 15 rotas estáticas no build (5 cases + sitemap + robots).
- [x] Navegação funciona em todas as páginas (navbar nos cantos, footer, skip link).
- [x] CTAs apontam ao destino certo (CTA primário = contato/ver projetos).
- [~] Placeholders de imagem sinalizados em `CONTENT_PENDING.md` (waiver de demo).

## Responsive
- [x] Desktop e mobile revisados via screenshots (1440px e 390px).
- [x] Sem overflow/sobreposição observado; composição de cantos colapsa para empilhamento no mobile.
- [ ] Tap targets >= 44px no mobile.

## Accessibility — target: **AA**
- [x] Headings semânticos, um H1 coerente por página (hero/header de cada rota).
- [x] Navegação completa por teclado; skip link "Pular para o conteúdo".
- [x] Foco visível em todos os interativos (`:focus-visible` outline `--accent`).
- [x] Contraste texto/fundo AA — tokens verificados (text 16:1, accent 8:1, muted ≈4.8:1).
- [x] Alt significativo em imagens (descrevem o espaço; em `lib/projects.ts`).
- [x] `prefers-reduced-motion` respeitado (CSS + Reveal + Lenis desligam).
- [x] Formulário com labels associados; estado de sucesso com `role=status` (validação HTML nativa).

## SEO — scope: **local/nacional (SC)**
- [x] Title + meta description únicos por página (metadata por rota + template).
- [x] Um H1 por página alinhado à intenção de busca.
- [x] Links internos coerentes; sem órfãs (galeria + "próximo projeto" encadeiam cases).
- [x] Open Graph por página/case (layout + generateMetadata do case).
- [x] Schema `ProfessionalService` + sitemap.xml + robots.txt.

## Performance — target: **CWV** (crítico, image-heavy)
- [x] Imagens via `next/image` (AVIF/WebP, sizes, lazy below-the-fold, blur placeholder, hero `priority`).
- [x] Fontes via `next/font` (subset latin, `display: swap`, só pesos usados — sem CLS).
- [x] JS enxuto (First Load ~87–103 kB); motion só transform/opacity; IO em vez de lib pesada.
- [~] LCP/CLS/INP: medir formalmente no deploy (Lighthouse) — estrutura favorável.
- [x] Sem erros de runtime no console (apenas aviso de HMR em dev).

## Design fidelity
- [x] Bate com tokens de `DESIGN.md` (cor, tipo, espaçamento) — single source em CSS vars + tailwind.
- [x] Regras de design honradas (galeria, não landing; UI nos cantos; flat; accent contido; grid assimétrico) — validado por screenshots.

## Final approval
- [x] Itens aplicáveis marcados; waivers de demo registrados em `DECISIONS_LOG.md`/`CONTENT_PENDING.md`.
- [~] Pronto para deploy (MVP) — falta plugar imagens reais e backend de formulário para produção.
